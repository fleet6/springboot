eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10011/eureka/

server:
  port: 10016
  
management:
  port: 100161

turbine:
  app-config: common-client,common-client1
  cluster-name-expression: new String("default")
  combine-host-port: true
