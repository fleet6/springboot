zuul:
  routes:
    eureka-server: /eureka/**
    config-center: /config/**
    common-client: /client/**
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10011/eureka/

server:
  port: 10012
